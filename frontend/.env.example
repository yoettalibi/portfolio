# Local dev — Vite proxy forwards /api to the Laravel dev server (php artisan serve)
VITE_API_URL=/api

# Production — set this to your actual domain when building for deployment
# VITE_API_URL=https://yourdomain.com/api
